import React from 'react'
import LoaderGif from '../../assets/loader.gif'

const Loader = (props) => (
  <div>
    <img 
      src={LoaderGif} 
      alt="Loader"
    />
    
  </div>
)

export default Loader